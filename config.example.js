/**
 * Configuration Template
 * 
 * IMPORTANT: 
 * - Copy this file to config.js and fill in your actual Supabase credentials
 * - NEVER commit config.js with real keys to Git (it's already in .gitignore)
 * 
 * Setup Instructions:
 * 1. Copy this file: cp config.example.js config.js
 * 2. Open config.js and replace YOUR_SUPABASE_URL and YOUR_SUPABASE_ANON_KEY with your actual values
 * 3. Get your credentials from: Supabase Dashboard → Settings → API
 */

const CONFIG = {
    // Your Supabase Project URL
    // Get this from: Supabase Dashboard → Settings → API → Project URL
    SUPABASE_URL: 'YOUR_SUPABASE_URL',
    
    // Your Supabase Anon/Public Key
    // Get this from: Supabase Dashboard → Settings → API → Project API keys → anon public
    SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY'
};
